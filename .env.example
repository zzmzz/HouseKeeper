# 运行时大模型（OpenAI 兼容端点）—— L3 意图兜底、纠正解析
EV_API_URL=https://your-endpoint/v1-compatible
EV_API_KEY=your-key-here
EV_MODEL=your-model-name

# 离线高阶任务走 `claude -p` 无头模式，用你已有的登录态，无需在此配置

# 可选：百度地图 ak（通勤时长脚本用；不配则该能力不可用）
EV_BAIDU_AK=
# 可选：HASS 命令行工具路径
EV_HASS_BIN=/path/to/hass-agent/bin/hass
